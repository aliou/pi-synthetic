---
"@aliou/pi-synthetic": patch
---

Update `hf:openai/gpt-oss-120b` to reflect current Synthetic API metadata: `provider` is now `synthetic` (Synthetic-hosted instead of proxied via fireworks) and `maxTokens` increased from 32768 to 65536.
