---
"@aliou/pi-synthetic": patch
---

Move `@mariozechner/pi-tui` to peer dependencies to avoid bundling the SDK alongside the extension. Fix `prepare` script to only run husky from a git repository.
