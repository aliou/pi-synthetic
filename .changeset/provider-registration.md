---
"@aliou/pi-synthetic": minor
---

Register the Synthetic provider as a complete pi-ai `Provider` via `pi.registerProvider(provider)` instead of the name-plus-config form, with auth resolution that falls back to an anonymous credential when no key exists.
